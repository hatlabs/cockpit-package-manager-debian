FROM debian:trixie-slim

# Install Debian packaging tools and Node.js
RUN apt-get update && \
    apt-get install -y \
        build-essential \
        debhelper \
        devscripts \
        git \
        ca-certificates \
        nodejs \
        npm \
        && rm -rf /var/lib/apt/lists/*

WORKDIR /build
